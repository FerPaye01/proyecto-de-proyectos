# Changelog de Skills

## 2026-02-26
- Actualizado desde: https://raw.githubusercontent.com/elastic/workflows/main/docs/concepts.md
- Cambios detectados: Investigación exhaustiva del repositorio oficial `elastic/workflows` a nivel de carpetas `/docs` y `/workflows/ai-agents/`. El skill ahora soporta y documenta la creación de árboles de decisión en los flujos mediante paso condicionales (`type: if` y llaves `condition`), el uso avanzado de inyección Liquid (`{{ event | json }}`), y provee el Action Type exacto para orquestar llamadas a Agentes de Agent Builder (`kibana.post_agent_builder_converse`), consolidando la integración nativa IA + Workflows en Elastic.
